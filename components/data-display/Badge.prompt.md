**Badge** — small uppercase pill for skills, project tech, and statuses.

```jsx
<Badge>Branding</Badge>
<Badge variant="solid">Nuevo</Badge>
<Badge variant="outline">Figma</Badge>
```

Variants: `soft` (pink tint + pink hairline — default), `solid` (gradient pink), `outline` (neutral hairline).
